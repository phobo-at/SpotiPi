# SpotiPi Environment Configuration Example
# Copy this file to ~/.spotify_wakeup/.env and fill in your values

# Spotify API Credentials
# Get these from: https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_client_id_here
SPOTIFY_CLIENT_SECRET=your_client_secret_here
SPOTIFY_REFRESH_TOKEN=your_refresh_token_here
SPOTIFY_USERNAME=your_spotify_username

# Flask Security (CRITICAL SECURITY FIX)
# Generate a secure secret key using: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your_secure_32_byte_hex_key_here

# Optional: Environment Override
SPOTIPI_ENV=development  # or 'production' - Overrides auto-detection

# Optional: Development Mode
SPOTIPI_DEV=1  # Set to 1 for full logging (overrides Pi auto-detection)

# Optional: Force Raspberry Pi Detection
# SPOTIPI_RASPBERRY_PI=1  # Force Pi behavior on non-Pi systems (for testing)

# Performance Tuning (Pi Zero W defaults)
# LOW_POWER mode enables optimizations for single-core ARM systems
# Automatically enabled on detected Pi hardware, but can be forced:
# SPOTIPI_LOW_POWER=1             # Enable low-power optimizations (default: auto-detect)
# SPOTIPI_MAX_CONCURRENCY=2       # Max worker threads (default: 2 on Pi, 4 elsewhere)
# SPOTIPI_JSON_LOGS=1             # JSON structured logging for journalctl (default: 1 on Pi)
# SPOTIPI_CONFIG_CACHE_TTL=30.0   # Config cache TTL in seconds (default: 30s Pi, 5s dev)
# SPOTIPI_LIBRARY_TTL_MINUTES=60  # Music library cache TTL (default: 60 min)
# SPOTIPI_DEVICE_DISK_PERSIST_SECONDS=600  # Device cache persist interval (default: 600s Pi)
# SPOTIPI_PLAYBACK_CACHE_TTL=5.0  # Playback status cache TTL (default: 5s Pi, 1.5s dev)
# SPOTIPI_HTTP_POOL_CONNECTIONS=5 # HTTP connection pool size (default: 5 Pi, 10 dev)
# SPOTIPI_HTTP_POOL_MAXSIZE=10    # HTTP pool max connections (default: 10 Pi, 20 dev)

# Threading Safety (Library Loading)
# SPOTIPI_LIBRARY_LOAD_TIMEOUT=20    # Total library load timeout in seconds (default: 20)
# SPOTIPI_LIBRARY_SECTION_TIMEOUT=15 # Per-section timeout in seconds (default: 15)

# Optional: Port Override
PORT=5001  # Default port for the application

# Optional: Alarm Reliability Tuning
# Seconds before the trigger window to pre-warm Spotify token/device caches (default 120)
# SPOTIPI_PREWARM_SECONDS=120
# Catch-up grace in seconds for missed alarms after NTP steps or reboots (default 600)
# SPOTIPI_CATCHUP_GRACE_SECONDS=600
# Override timezone detection for alarms (default Europe/Vienna)
# SPOTIPI_TIMEZONE=Europe/Vienna
# Network/DNS probes for readiness checks
# SPOTIPI_NETWORK_PROBE_HOST=api.spotify.com
# SPOTIPI_NETWORK_PROBE_TIMEOUT=1.5
# SPOTIPI_DNS_PROBE_HOST=api.spotify.com
